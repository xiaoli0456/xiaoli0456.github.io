关于您的疑问：**此次重构完全不会影响 SEO，也不会影响 `pnpm generate` 打包后的 HTML 静态文件。**

**原因如下：**
1.  **构建机制**：Nuxt/Vite 在打包时会解析所有资源引用。只要代码中的路径同步更新（这是重构的一部分），构建工具会自动处理文件指纹（Hashing）和路径映射，生成的最终 HTML 会正确指向新位置的资源。
2.  **HTML 结构不变**：组件目录的调整仅涉及开发环境的文件组织。通过配置 `pathPrefix: false`，组件在最终渲染时的逻辑和生成的 DOM 结构完全保持不变，因此搜索引擎爬虫看到的页面内容与重构前完全一致。

---

### 执行计划：项目目录重构

我将按照以下步骤执行重构，确保零副作用：

#### 1. 资源目录优化 (Assets)
- **创建目录**：
  - `src/assets/images/banners`
  - `src/assets/images/common`
  - `src/assets/styles`
- **移动文件**：
  - 将 Banner 图片移至 `banners` 目录。
  - 将 Logo 等通用图片移至 `common` 目录。
  - 将 `src/style.css` 移至 `src/assets/styles/main.css`。

#### 2. 组件目录结构化 (Components)
- **创建目录**：
  - `src/components/layout/`
  - `src/components/home/`
  - `src/components/common/`
- **移动组件**：
  - **Layout**: `AppHeader.vue`, `AppFooter.vue`, `MainContent.vue`, `CategoryPageLayout.vue`
  - **Home**: `HeroBanner.vue`, `Carousel.vue`
- **配置 Nuxt**：
  - 修改 `nuxt.config.js`，设置 `components: [{ path: '~/components', pathPrefix: false }]`，确保无需修改引用组件的名称。

#### 3. 清理与更新
- **删除冗余**：删除 `src/components/HeroBanner copy.vue`（已确认无代码引用）。
- **更新引用**：
  - 全局扫描并更新所有 `.vue` 和 `.ts` 文件中的图片引用路径（如 `../assets/banner1.jpg` -> `@/assets/images/banners/banner1.jpg`）。
  - 更新 `nuxt.config.js` 中的 CSS 引用为 `~/assets/styles/main.css`。

#### 4. 验证
- 运行 `pnpm dev` 确保开发环境正常。
- 检查页面是否正常加载图片和样式。

请批准执行。
