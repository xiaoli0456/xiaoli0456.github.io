我建议对项目目录进行以下重构，以提高代码的可维护性和清晰度：

## 1. 资源目录优化 (Assets)
将 `src/assets` 下散乱的文件进行分类：
- 创建 `src/assets/images/banners`：存放所有 banner 图片。
- 创建 `src/assets/images/common`：存放通用图片（如 logo 等）。
- 创建 `src/assets/styles`：将 `src/style.css` 移至此处，并重命名为 `main.css`。

## 2. 组件目录结构化 (Components)
目前的组件全部平铺在根目录下，建议按功能分组：
- **Layout 组件** (`src/components/layout/`)：存放 `AppHeader.vue`, `AppFooter.vue`, `MainContent.vue`, `CategoryPageLayout.vue`。
- **Home 组件** (`src/components/home/`)：存放 `HeroBanner.vue`, `Carousel.vue` 等首页相关组件。
- **Common 组件** (`src/components/common/`)：存放通用 UI 组件（如果有）。

**关键配置调整**：
为了避免移动文件后需要修改大量代码中的组件引用（例如 `<AppHeader>` 变成 `<LayoutAppHeader>`），我将修改 `nuxt.config.js`，设置组件的 `pathPrefix: false`。这样你可以继续使用原有的组件名称，无需修改 `.vue` 文件中的模板代码。

## 3. 清理冗余文件
- 删除 `src/components/HeroBanner copy.vue`（这是一个备份文件）。

## 4. 配置文件更新
- 更新 `nuxt.config.js` 中的 CSS 引用路径。
- 自动扫描并更新任何硬编码的图片引用路径（如果代码中有 `import ... from '../assets/banner1.jpg'` 这种写法，我会一并更新）。

请确认是否执行此重构计划？
